MCP_PORT=3001
MCP_HOST=127.0.0.1
# npm workspace scripts run inside mcp/, so this default targets the repository root.
MCP_WORKSPACE_ROOT=..
MCP_PREVIEW_BASE_URL=http://127.0.0.1:3000
MCP_MAX_FILES_PER_WRITE=25
MCP_MAX_FILE_BYTES=524288
MCP_CHECK_TIMEOUT_MS=300000
